# Server (localhost)
PORT=5000
HOST=localhost

# MongoDB (use 127.0.0.1 to avoid IPv6 ::1 connection issues on Windows)
MONGO_URI=mongodb://127.0.0.1:27017/mbm

# JWT (use a strong secret in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES=7d

# Admin seeder (optional - run: npm run seed)
# ADMIN_NAME=Admin
# ADMIN_EMAIL=admin@lishnutech.com
# ADMIN_PASSWORD=admin123
